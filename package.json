{
  "name": "twilioVoiceRecordingToS3",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "aws-sdk": "^2.884.0",
    "axios": "^0.21.1",
    "s3-upload-stream": "^1.0.7",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  }
}
